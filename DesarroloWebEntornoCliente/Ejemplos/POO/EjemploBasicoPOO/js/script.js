/*import Coche from "./Coche.js";

document.addEventListener("DOMContentLoaded", () => {

    const persona = {

        nombre: "Juan",

        saludar() {

            document.body.innerHTML += `<p>Hola! ${persona.nombre}</p>`;

        }

    }

    persona.saludar();

    /*function Coche(marca, modelo) {

        this.marca = marca;

        this.modelo = modelo;

    }

    const coche1 = new Coche(`Audi`, `900`);

    document.body.innerHTML += `La marca es ${coche1.marca}`;

    const coche1 = new Coche(`Marca del coche`, `Modelo del coche`);

    const coche2 = new Coche(`Audi`, `Galicia`);

    coche1.mostrarDatos();

    coche2.mostrarDatos();
    

});*/